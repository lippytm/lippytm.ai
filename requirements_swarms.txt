# lippytm Life/Business Model AI Swarms
# Install: pip install -r requirements_swarms.txt

# Core AI
anthropic>=0.40.0          # Claude API with prompt caching + extended thinking

# Data & utilities
pydantic>=2.0.0            # Data validation for business models
python-dotenv>=1.0.0       # .env file loading for API keys

# Optional: enhanced output
rich>=13.0.0               # Formatted terminal output

# Optional: persistent memory / vector search
# chromadb>=0.5.0          # Local vector DB for agent memory
# pinecone-client>=3.0.0   # Cloud vector DB

# Optional: web integrations
# httpx>=0.27.0            # Async HTTP for live data fetching
# github-python>=2.0.0     # GitHub API client
